// Local defaults template. Copy to extension/config.js and fill in.
// config.js is gitignored — the copy is what gets baked into your unpacked
// extension; it re-seeds chrome.storage.local on every service-worker start,
// so keys survive "Load unpacked" reloads without re-pasting in the popup.

export const DEFAULT_GSB_KEY = "";
export const DEFAULT_BACKEND_URL = "";
export const DEFAULT_IPINFO_TOKEN = "";
export const DEFAULT_REPORT_TOKEN = "";
